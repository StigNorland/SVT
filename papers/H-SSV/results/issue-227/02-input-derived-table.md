# Issue #227 — input and derived-quantity ledger

| item | status | reason |
|---|---|---|
| C4 local causal gates and finite positive capacities | inherited input | Passed issue #226. |
| Two-dimensional spatial antichain connectivity | inherited structural input | Part of the C4 screen ontology. |
| Measured `G` | empirical input | The local Gauss construction fixes form, not microscopic magnitude. |
| `ell_*`, `m_*`, `nu_*`, `tau_R` | T3 global inputs | Not calculated by C4. |
| Compact minimum patch | T3 postulate | Needed for area and boundary isoperimetry. |
| Saturated boundary cut | T3 postulate | Needed for throughput proportional to `sqrt(M)`. |
| `r_c proportional sqrt(M)` | conditional derivation | Follows from compact filling with universal `ell_*` and `m_*`. |
| `V_inf^4 proportional M` | conditional derivation | Follows from positive min-cut scaling. |
| `a_s=a_c=a_*` | added closure | Continuity does not prove microscopic equality. |
| Numerical `a_*` | not derived | No pre-galaxy value follows from C4/T3. |
| Cored-log kernel | not derived | A Gaussian positive kernel survives the same controls. |
| Host/satellite shared-screen response | not derived | C4 permits correlation but fixes no remote marginal response law. |
| Screen for every coherent subsystem (C5) | post-result hypothesis | Requires objective coherence, nesting, one-count capacity, and composition rules. |
| Binary radiation and orbital loss | not derived | No dynamical response action exists. |
| `c_screen=c` at measured precision | not derived | C4 supplies only a causal upper bound. |

The two principal formulas are therefore admissible only with their conditions
shown:

\[
r_c=\sqrt{GM/a_*},\qquad V_\infty^4=GMa_*.
\]

They contain no per-galaxy halo fit, but they are not frozen predictions until
`a_*` and a regulator are fixed independently of galaxy outcomes.
