# Invariant screen-redshift audit

This note records the strongest cosmological propagation structures compatible
with the C4 foundation. It does not derive a viable cosmology.

## Endpoint observable and lapse no-go

A photon frequency measured by an observer with four-velocity `u` is
`omega=-u dot k`. Therefore

\[
1+z={(-u\cdot k)_e\over(-u\cdot k)_o}.
\]

For

\[
ds^2=-A(t)^2c^2dt^2+d\mathbf x^2,
\]

the substitution `d tau=A dt` gives Minkowski space. Equivalent comoving
clocks measure `1+z=1`; the endpoint ratio `A_o/A_e` is not a cosmological
redshift.

## Non-geometric propagation controls

An energy-only comparison law,

\[
d\ln\nu/dD=-\kappa,
\qquad 1+z=e^{\kappa D},
\]

can be achromatic if `kappa` is frequency-independent. In a stationary channel
it does not change the separation of successive photons: its duration factor is
one. With conserved photon number in a static Euclidean baseline it has
`D_L=D sqrt(1+z)`, surface-brightness exponent `-1`, and violates distance
duality.

An ideal coherent wavepacket dilation,

\[
\psi_o(t)=y^{-1/2}\psi_e(t/y),\qquad y=1+z,
\]

does give `omega_o=omega_e/y` and `Delta t_o=y Delta t_e` without angular blur.
But without a spatial beam response it gives `D_L=yD`, `D_A=D`, and
`D_L/(y^2D_A)=1/y`; its surface-brightness exponent is `-2`, not the metric
`-4`. A scalar statement that the lost photon energy enters the screen closes
arithmetic but does not supply the Hamiltonian that preserves wavepacket
coherence while recording energy.

## Optical completion and identifiability

The missing response can be written as a physical spatial scale,

\[
ds^2=-A(t)^2c^2dt^2+B(t)^2d\mathbf x^2,
\qquad 1+z={B_o\over B_e}.
\]

This recovers duration stretching, distance duality, and Tolman scaling. It is
a geometric expansion law; calling `B` a screen observable does not make it a
distinct measured redshift component.

If a separate wake factor `W` is retained, ideal electromagnetic observables
depend on

\[
S=BW,qquad H_{\rm eff}=\dot S/S=H_{\rm geom}+\Gamma_{\rm wake}.
\]

For any function `f(t)`, `B -> B exp(f)` and `W -> W exp(-f)` leave `S`, redshift,
duration, optical distances, and redshift drift unchanged. The two response
columns are identical and have rank one. A screen fraction cannot be inferred
by fitting more electromagnetic data to this product; C4 must predict an
independent non-electromagnetic coupling or distinct matter/ruler response.

## Information-area expansion R4

The post-preregistration R4 proposal makes the screen expansion itself the
geometry. If persistent record occupancy `N` has fixed area per occupied cell,

\[
\mathcal A_s=a_{\rm cell}N,
\qquad B=\sqrt{N/N_0},
\qquad 1+z=\sqrt{N_o/N_e}.
\]

It follows that

\[
H_B={1\over2}{\dot N\over N},qquad
{\ddot B\over B}={1\over2}{\ddot N\over N}
-{1\over4}\left({\dot N\over N}\right)^2.
\]

Constant record production gives `q_B=1`, and `N proportional t^p` gives
`q_B=2/p-1`. The requested deceleration is therefore a falsifiable prediction
for `p<2`, not a verbal property.

R4 must not identify `N` with newly created global information: C4's unitary
global ledger preserves that information. `N` must instead be a reduced-state
persistent-record occupancy or activation of latent capacity. The persistence
criterion, cell area, area stress/energy, site activation, `N(t)`, saturation,
and perturbation dynamics are all missing. A physically expanding `B(t)` can
produce spacetime curvature even with intrinsically flat spatial slices;
spatial curvature and expansion are not interchangeable assumptions.

## Cosmological gate

A uniform frequency dilation can preserve a Planck spectrum conditionally if
frequency and temperature scale together and phase-space occupation is
preserved. That kinematic fact does not derive the CMB origin, acoustic ruler,
recombination, nucleosynthesis, or structure growth. Likewise, redshift drift

\[
\dot z=(1+z)H_{{\rm eff},o}-H_{{\rm eff},e}
\]

still depends only on the effective sum unless R4 supplies independent matter
dynamics.

No candidate passes all C1--C6. Pure decelerating R4 is additionally in tension
with the registered late-time standard-candle acceleration test under its
flatness and homogeneity assumptions. Cosmic age, lookback time, horizon, and
size calculations remain prohibited.
