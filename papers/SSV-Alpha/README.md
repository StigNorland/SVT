# SSV Alpha

Focused draft on the fine-structure constant as a toroidal-vortex aspect-ratio problem.

Core claim:

```tex
R^*/\xi = \alpha^{-1}
```

The draft does not claim to derive `\alpha`; it frames the calculation needed to derive it from the 3D LogSE plus chiral-shear toroidal-vortex minimisation.
