\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.5cm]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,physics}
\usepackage{booktabs}
\usepackage{float}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{enumitem}
\providecommand{\ssvissue}[1]{\hyperref[issue:#1]{\##1}}% in-text issue ref -> Code & Issue References appendix
\providecommand{\ssvfile}[1]{\hyperref[file:#1]{\texttt{\detokenize{#1}}}}% in-text script/report ref -> same appendix
\usepackage{caption}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}

\newtcolorbox{resultbox}[1][]{enhanced,breakable,
  colback=blue!4!white,colframe=blue!40!black,
  fonttitle=\bfseries,title={#1},boxrule=0.6pt,arc=4pt}
\newtcolorbox{gapbox}[1][]{enhanced,breakable,
  colback=orange!6!white,colframe=orange!60!black,
  fonttitle=\bfseries,title={#1},boxrule=0.6pt,arc=4pt}

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

\title{\textbf{Saturated Superfluid Vacuum~VII-a}\\[0.4em]
\large Quantum Mechanics from Hydrodynamics}
\author{Stig Norland\\
\small Independent Researcher, Bergen, Norway}
\date{Draft}

\begin{document}
\maketitle

\begin{abstract}
This paper derives the quantum-mechanical limit of the Saturated Superfluid
Vacuum (SSV) framework.  The scope is deliberately narrow: the Madelung
transformation, the linear Schr\"odinger limit, uncertainty as a hydraulic
localisation bound, and measurement as a physical reconnection channel.

The formal result is the recovery of the Schr\"odinger equation from the
low-amplitude hydrodynamic regime of the SSV medium.  Quantisation enters as
the single-valuedness condition on the order-parameter phase, tying discrete
winding numbers to circulation topology rather than to a separate quantisation
rule.  Uncertainty is interpreted as the hydraulic cost of localising the
order parameter.  Measurement is modelled as an irreversible reconnection or
wake-writing event in the medium.

Claims about emergent geometry, Einstein equations, cosmogony, and full-series
synthesis are left to companion papers.
\end{abstract}

\newpage
\tableofcontents
\newpage

%%──────────────────────────────────────────────────────────────────────────────
\section{Introduction}
\label{sec:intro}
%%──────────────────────────────────────────────────────────────────────────────

The SSV programme treats the vacuum as a saturated superfluid described by a
complex order parameter $\Psi$.  Papers~I--IV establish the medium, its
topological defects, thermodynamic arrow, and gravitational limit.  Paper~V
extends the same picture to strong gravity and condensate black holes.  The
present paper asks a narrower question: under what conditions does ordinary
quantum mechanics appear as the low-amplitude hydrodynamic limit of this medium?

The working thesis is that quantum behaviour is not an additional foundational
axiom.  It is the wave-mechanical regime of the order parameter when
perturbations are small, gradients are gentle, and defects can be treated
through an effective potential.  In that regime the Madelung variables map the
complex wave equation to continuity plus Euler-type flow equations, and the
inverse map recovers the Schr\"odinger equation for a slowly varying envelope.

This paper separates three claim levels explicitly.

\begin{enumerate}[label=\textbf{C\arabic*.}]
  \item \textbf{Formal derivation.}  The Madelung transformation and its inverse
    show how a Schr\"odinger-type equation is equivalent to hydrodynamic flow
    with a quantum-pressure term.  This is an exact algebraic equivalence, not
    an approximation.

  \item \textbf{Controlled limit.}  In the low-amplitude, long-wavelength regime,
    the nonlinear medium terms can be absorbed into background phase and
    effective potentials, leaving the ordinary linear Schr\"odinger equation.
    This is the limit that makes SSV contact with standard quantum mechanics.

  \item \textbf{Physical interpretation.}  Quantisation, uncertainty, and
    measurement are read as circulation topology, localisation stiffness, and
    irreversible reconnection respectively.  These are model commitments that
    arise naturally from the medium picture; they are not independent mathematical
    theorems.
\end{enumerate}

\subsection*{Roadmap}

Section~\ref{sec:madelung} carries out the Madelung transformation and derives
the hydrodynamic equations.  Section~\ref{sec:schrodinger} takes the
Schr\"odinger limit and shows how quantisation follows from phase topology.
Section~\ref{sec:uncertainty} interprets the uncertainty relation as a hydraulic
localisation bound.  Section~\ref{sec:measurement} models measurement as a
reconnection event and discusses Born weights.  Section~\ref{sec:discussion}
places the results in the context of the series and lists open calculations.

%%──────────────────────────────────────────────────────────────────────────────
\section{Madelung Transformation}
\label{sec:madelung}
%%──────────────────────────────────────────────────────────────────────────────

\subsection{Polar decomposition}

Let the local state of the saturated medium be the complex order parameter
\begin{equation}
  \Psi(\mathbf{x},t) = \sqrt{\rho(\mathbf{x},t)}\,e^{iS(\mathbf{x},t)/\hbar},
  \label{eq:polar}
\end{equation}
where $\rho$ is the mass density and $S$ is the phase action.

\paragraph{Normalisation convention, stated once (\ssvissue{189}).}
$\rho$ is used in two conventions in this paper and the difference is recorded
here rather than left to the reader.  In the hydrodynamic sections
\eqref{eq:continuity}--\eqref{eq:euler} and in the Schr\"odinger limit,
$\rho = |\Psi|^2$ is a \emph{mass density} and $\rho_0$ is the saturated
background of Papers~I--IV.  In \S\ref{sec:gausson-saturation} the
single-particle convention $\int|\Psi|^2\,\mathrm{d}x = 1$ is used instead,
without which \eqref{eq:gausson-Dx} would not give $\sigma^2/2$; there
$|\Psi|^2$ is a probability density and $\rho_0$ is whatever reference
density makes the logarithm's argument dimensionless.  The two differ by a
factor of the particle mass and a normalisation volume.  Nothing in
\eqref{eq:continuity}--\eqref{eq:euler} depends on the choice --- $Q$ in
\eqref{eq:Q} is invariant under $\rho\to\lambda\rho$ --- but the symbol does
carry two dimensions across the paper, and saying so is cheaper than the
alternative.  Generalised as \ssvissue{205}.

The associated
irrotational velocity field is
\begin{equation}
  \mathbf{v} = \frac{1}{m}\nabla S.
  \label{eq:velocity}
\end{equation}

For the logarithmic-Schr\"odinger (LogSE) medium of Papers~I--IV~\cite{SSV-I,SSV-IV},
the order parameter obeys
\begin{equation}
  i\hbar\,\partial_t\Psi =
  \left[-\frac{\hbar^2}{2m}\nabla^2 + V_\mathrm{ext} + U(\rho)\right]\Psi,
  \label{eq:medium_wave}
\end{equation}
where $V_\mathrm{ext}$ is a slowly varying effective potential and $U(\rho)$
is the medium self-interaction.  The specific equation of state is fixed in
earlier papers; the present paper uses only the hydrodynamic structure common
to this class of order-parameter equations.

\subsection{Hydrodynamic form}

Substituting \eqref{eq:polar} into \eqref{eq:medium_wave} and separating real
and imaginary parts gives the Madelung pair
\begin{align}
  \partial_t\rho + \nabla\cdot(\rho\mathbf{v}) &= 0,
  \label{eq:continuity}\\
  \partial_t S + \frac{(\nabla S)^2}{2m} + V_\mathrm{ext} + U(\rho) + Q &= 0,
  \label{eq:hamilton_jacobi}
\end{align}
with the quantum pressure
\begin{equation}
  Q(\mathbf{x},t) = -\frac{\hbar^2}{2m}\frac{\nabla^2\sqrt{\rho}}{\sqrt{\rho}}.
  \label{eq:Q}
\end{equation}
Taking a gradient of \eqref{eq:hamilton_jacobi} gives the Euler equation
\begin{equation}
  m\bigl(\partial_t + \mathbf{v}\cdot\nabla\bigr)\mathbf{v}
  = -\nabla\bigl(V_\mathrm{ext} + U(\rho) + Q\bigr).
  \label{eq:euler}
\end{equation}

\begin{resultbox}[Madelung equivalence]
Equations~\eqref{eq:continuity}--\eqref{eq:euler} are the Madelung equations:
compressible irrotational hydrodynamics with an additional quantum-pressure
contribution $Q$.  The transformation is exact and bidirectional: starting from
the wave equation \eqref{eq:medium_wave} yields the hydrodynamic pair; starting
from the hydrodynamic pair with the irrotationality condition~\eqref{eq:velocity}
reconstructs the wave equation.
\end{resultbox}

%%──────────────────────────────────────────────────────────────────────────────
\section{Schr\"odinger Limit}
\label{sec:schrodinger}
%%──────────────────────────────────────────────────────────────────────────────

\subsection{Low-amplitude long-wavelength limit}

Write the density as $\rho = \rho_0 + \delta\rho$, with $|\delta\rho|\ll\rho_0$,
and assume gradients are long compared with the healing length of the medium.
Expand the self-interaction around the saturated background:
\begin{equation}
  U(\rho) = U(\rho_0) + U'(\rho_0)\,\delta\rho + \cdots
  \label{eq:U_expand}
\end{equation}
The constant term shifts the global phase; the linear term is either negligible
for a sufficiently weak perturbation or is absorbed into an effective potential.
The slowly varying envelope $\psi$ then obeys
\begin{equation}
  i\hbar\,\partial_t\psi =
  \left[-\frac{\hbar^2}{2m}\nabla^2 + V_\mathrm{eff}\right]\psi,
  \label{eq:schrodinger}
\end{equation}
the ordinary Schr\"odinger equation for the effective degree of freedom.

\begin{resultbox}[Schr\"odinger limit]
The SSV medium in its low-amplitude, long-wavelength regime reduces exactly to
linear Schr\"odinger dynamics.  The claim is not that all of quantum theory is
derived in one step; it is that whenever the conditions above hold, the same
hydrodynamic variables reduce to the linear dynamics used in standard quantum
mechanics.
\end{resultbox}

\paragraph{Origin of $V_\mathrm{eff}$, and the hydrogen spectrum.}
\label{para:Veff-hydrogen}
The effective potential in \eqref{eq:schrodinger} is not imported from
elsewhere; it is the linearised long-wavelength shear field around the source
defect.  For a heavy stationary source defect with topological chirality
(Paper~I, electric-charge identification), the chiral-shear sector of the
SSV Lagrangian propagates the source's chirality outward at the transverse
phase speed $c_\perp = \alpha c$.  At distances large compared with the
source healing length and small compared with the cosmological horizon, the
linearised chiral-shear potential reduces to the Coulomb form
\begin{equation}
  V_\mathrm{eff}(r) = -\,\frac{e^2}{4\pi\varepsilon_0\,r},
  \label{eq:Veff-coulomb}
\end{equation}
where the proportionality is fixed by the same identification
$\alpha = c_\perp/c$ that defines the chiral-shear coupling in
Paper~I (\S\,Postulate).

\emph{Correction of attribution (\ssvissue{189}).}  Earlier versions of this
paragraph credited the reduction to Coulomb form to ``Paper~I's
electromagnetism sector''.  It is not there.  Paper~I fixes
$\alpha = c_\perp/c$ and states that the chiral stiffness acts ``in the
statics of charged defects'', explicitly routing that sector to
Paper~II~\cite{SSV-II}; the Bernoulli-pressure derivation of
$F_C = \alpha\hbar c/r^2$ is Paper~II's.  The two papers were both cited for
one claim that only one of them makes.

It should also be said plainly what \eqref{eq:Veff-coulomb} costs.  Paper~II
records the Coulomb coupling as \emph{empirical} $\alpha$: the $1/r$ form is
derived, the strength is an input.  So what follows is a
\emph{consistency} result --- the Rydberg series is recovered given
$\{m_e,\alpha,c\}$, which the series already concedes as inputs --- and not a
prediction of the Rydberg constant.

Equation~\eqref{eq:schrodinger} with
$V_\mathrm{eff}$ from \eqref{eq:Veff-coulomb} is the hydrogen Schr\"odinger
equation; its eigenvalues are the familiar Rydberg series
\begin{equation}
  E_n = -\frac{m_e c^2 \alpha^2}{2 n^2}
       \approx -\frac{13.6\,\mathrm{eV}}{n^2},
  \label{eq:rydberg}
\end{equation}
with the prefactor expressed entirely in SSV constants $\{m_e, \alpha, c\}$
that Paper~I already takes as fixed.  The hydrogen-like bound-state spectrum
therefore follows from the LogSE low-amplitude long-wavelength limit together
with the chiral-shear identification of electromagnetism (Paper~I for
$\alpha=c_\perp/c$, Paper~II~\cite{SSV-II} for the Coulomb statics), adding no
input beyond the ones already conceded.  ``No fresh input'' is the correct
claim; ``derived'' is not, since $\alpha$ enters
\eqref{eq:Veff-coulomb} empirically.

The same construction applies to any heavy SSV source defect.  The effective
potential is set by the long-wavelength chiral-shear field of that source,
and the bound-state spectrum follows by the standard Schr\"odinger calculation
in \eqref{eq:schrodinger}.  The LogSE programme does not need to recompute
hydrogen; it needs to verify that \eqref{eq:Veff-coulomb} is the correct
linearised field, which is the content of Paper~I's electromagnetism sector.

\subsection{Quantisation as circulation topology}

The phase in \eqref{eq:polar} must be single-valued around any closed loop
that does not cross a zero of $\Psi$.  Therefore
\begin{equation}
  \oint_C \nabla S\cdot d\mathbf{l} = 2\pi n\hbar, \qquad n\in\mathbb{Z}.
  \label{eq:phase_quantisation}
\end{equation}
Using \eqref{eq:velocity}, this is the circulation condition
\begin{equation}
  \oint_C \mathbf{v}\cdot d\mathbf{l} = \frac{2\pi n\hbar}{m}.
  \label{eq:circulation}
\end{equation}
Quantisation is therefore tied to the topology of the phase field.  It is not
inserted as a separate rule for allowed orbits; it is the consequence of a
single-valued order parameter with vortex cores acting as excluded phase
singularities.  The detailed spectrum of any bound system depends on its
effective potential and boundary conditions, but the origin of discrete winding
numbers is hydrodynamic.  This is consistent with the topological spin derivation
of Paper~V~\cite{SSV-V} and the vortex-defect picture of Paper~I~\cite{SSV-I}.

%%──────────────────────────────────────────────────────────────────────────────
\section{Uncertainty as Hydraulic Bound}
\label{sec:uncertainty}
%%──────────────────────────────────────────────────────────────────────────────

The standard uncertainty relation follows from Fourier analysis of wave packets:
\begin{equation}
  \Delta x\,\Delta p \geq \frac{\hbar}{2}.
  \label{eq:uncertainty}
\end{equation}
SSV adds a physical interpretation of the same relation.  A perturbation
localised to a short length scale must include large gradients of $\sqrt{\rho}$
and phase.  Through \eqref{eq:Q}, those gradients carry a quantum-pressure cost
$\Delta E_Q \sim \hbar^2/(2m\,\Delta x^2)$.  Pushing $\Delta x$ downward
therefore forces a larger spread in momentum modes and a larger hydraulic
stiffness penalty, arriving at \eqref{eq:uncertainty}.

This section does not replace the standard proof.  The proof remains the
wave-packet Fourier theorem.  The SSV claim is that the theorem has a
medium-level interpretation: localisation is limited because the order parameter
cannot be squeezed arbitrarily without exciting the pressure and phase-gradient
channels of the fluid.  The uncertainty principle is the hydraulic stiffness of
the vacuum medium made quantitative.

\subsection{Saturation by the Gausson}
\label{sec:gausson-saturation}

\begin{gapbox}[Rejected-branch record --- this section is not current
(\ssvissue{189})]
\textbf{This section is retained deliberately, as a record of a route that
does not work.}  It is kept rather than deleted for the same reason
Paper~I keeps its rejected sign visible: a negative result that is explicit
in one place and invisible in another is the defect the \ssvissue{182} audit
was created to find.  \textbf{Nothing below should be read as a current claim
of the programme.}  Both findings are now machine-checked
(\ssvfile{logse_gaussian}; report \ssvfile{logse-gaussian-issue189}).

\begin{enumerate}[label=(\roman*)]
  \item \textbf{The Gausson does not exist on the adopted branch.}
        Substituting the Gaussian ansatz into the LogSE and requiring the
        $x^2$ terms to cancel fixes the width uniquely, and its \emph{sign} is
        set by the sign of the logarithmic term.  In the convention this
        section prints, $\sigma^2 = +\hbar^2/(2mb)$ and
        Eq.~\eqref{eq:gausson} is correct.  In the convention Paper~I adopted
        (\ssvissue{183}), $\sigma^2 = -\hbar^2/(2mb) < 0$ and \textbf{no
        normalisable Gaussian exists at all}.  Paper~I rejected the attractive
        branch because it makes the uniform vacuum modulationally unstable and
        cannot support $c_s=c$.  On the branch actually adopted,
        \S\ref{sec:gausson-saturation} has no solution to stand on, and
        particles must be \emph{topological} rather than bright solitons.

        The distinction matters for what is being conceded: this section did
        not miscalculate.  It wrote a correct equation for a theory the series
        has since rejected.
  \item \textbf{The $\hbar/2$ is imported, not derived --- on either branch.}
        The claim below is that the prefactor comes ``directly from the LogSE
        itself''.  The offered evidence is that the result is independent of
        the Gausson width $\sigma$, and therefore of $b$.  That independence is
        the tell: \emph{every} normalised Gaussian saturates
        $\Delta x\,\Delta p = \hbar/2$, for any width, with $b$ entering
        nowhere.  It is a property of Gaussians, not of the LogSE.  The
        computation performed --- Gaussian moments giving $\sigma^2/2$ and
        $\hbar^2/2\sigma^2$, then multiplying --- \emph{is} the standard
        wave-packet calculation.  The LogSE contributed only the claim that its
        stationary state is Gaussian, and under (i) it has no such state.

        That saturation is specific to Gaussianity rather than to
        normalisability is checked against a control: the normalised state
        $\psi \propto e^{-|x|/\lambda}$ gives
        $\Delta x\,\Delta p = (\sqrt2/2)\,\hbar > \hbar/2$.  Without that
        control the observation would be empty.
\end{enumerate}
The uncertainty principle may still be recoverable as ``hydraulic stiffness of
the vacuum'' in the sense this paper intends, but not by this route.  Whether a
\emph{topological} defect can do what the bright soliton cannot is open, and
shares a root with \ssvissue{196} and \ssvissue{178}: the bare one-component
order parameter may not carry the required structure, in which case the
replacement is a change of theory rather than a substitution.
\end{gapbox}

The remainder of this section is reproduced \emph{as it stood}, and is the
rejected-branch material referred to above.

The precise prefactor $1/2$ in \eqref{eq:uncertainty} was taken to follow
directly from the LogSE itself, without importing it from the standard
wave-packet calculation, by computing the uncertainty product on the LogSE's
natural coherent state.  The logarithmic Schr\"odinger equation
\begin{equation}
  i\hbar\,\partial_t\Psi
   = -\frac{\hbar^2}{2m}\,\nabla^2\Psi
     - b\,\Psi\,\ln(|\Psi|^2/\rho_0)
  \label{eq:LogSE-VIIa}
\end{equation}
admits an exact Gaussian stationary solution, the \emph{Gausson} of
Bialynicki-Birula and Mycielski~\cite{bbm1976}:
\begin{equation}
  \Psi_G(x,t)
   = \frac{1}{(\pi\sigma^2)^{1/4}}\,
     \exp\!\left(-\frac{x^2}{2\sigma^2}\right)\,
     \exp\!\left(-i\,E_G\,t/\hbar\right),
  \qquad
  \sigma^2 = \frac{\hbar^2}{2 m\,b},
  \label{eq:gausson}
\end{equation}
where the Gausson width $\sigma$ is fixed by the LogSE structure constants
$m$ and $b$.  Unlike the linear-Schr\"odinger Gaussian, the Gausson is a
\emph{true soliton}: the Gaussian shape is preserved exactly under LogSE
evolution; this is the defining property that singles out the logarithmic
nonlinearity~\cite{bbm1976}.

The position and momentum variances on the Gausson are evaluated directly
from Gaussian integrals:
\begin{align}
  (\Delta x)^2
   &= \int x^2\,|\Psi_G|^2\,\mathrm{d}x
    = \frac{\sigma^2}{2},
   \label{eq:gausson-Dx}
   \\
  (\Delta p)^2
   &= \int \big|\!-i\hbar\,\partial_x\Psi_G\big|^2\,\mathrm{d}x
    = \frac{\hbar^2}{2\sigma^2},
   \label{eq:gausson-Dp}
\end{align}
where the second line uses
$\partial_x\Psi_G = -(x/\sigma^2)\,\Psi_G$ and a single Gaussian integral.
The product is
\begin{equation}
  \Delta x\,\Delta p\big|_{\rm Gausson}
   = \sqrt{\frac{\sigma^2}{2}}\cdot\sqrt{\frac{\hbar^2}{2\sigma^2}}
   = \frac{\hbar}{2},
  \label{eq:gausson-saturation}
\end{equation}
which is independent of the Gausson width $\sigma$ and therefore independent
of the LogSE coupling $b$.

\begin{gapbox}[Withdrawn: the $\hbar/2$ prefactor is imported, not derived]
\textbf{This replaces the result this section originally claimed.}  The
superseded statement was that ``the $\hbar/2$ prefactor is derived, not
imported'' --- that the minimum uncertainty product is realised on the Gausson
as the LogSE's natural coherent state, with the structure constants $(m,b)$
fixing the width and hence the bound.

Both halves fail.  Under (i) the LogSE has no normalisable Gaussian on the
adopted branch, so there is no such coherent state; and under (ii) the width
$\sigma$ cancels out of \eqref{eq:gausson-saturation} identically, so
$(m,b)$ cannot be fixing anything the answer depends on.  The calculation
performed in \eqref{eq:gausson-Dx}--\eqref{eq:gausson-saturation} is the
textbook Gaussian wave-packet result, and it returns $\hbar/2$ whether or not
the LogSE is anywhere in the problem.

What \S\ref{sec:uncertainty} still supports is the \emph{form} of an
uncertainty relation from hydraulic stiffness.  The \emph{prefactor} is not
earned here.  Verdict \texttt{MISDERIVED}, recorded in
\ssvfile{logse-gaussian-issue189} and guarded against silent re-upgrade by
\ssvfile{logse_gaussian}.
\end{gapbox}

%%──────────────────────────────────────────────────────────────────────────────
\section{Measurement as Reconnection}
\label{sec:measurement}
%%──────────────────────────────────────────────────────────────────────────────

\subsection{Irreversibility from topology change}

The measurement argument is kept narrow.  A measurement is a physical interaction
that correlates a microscopic degree of freedom with a macroscopic apparatus by
driving some part of the combined medium into an irreversible reconnection or
wake-writing event.

Before measurement, a superposition is represented by a coherent field
configuration,
\begin{equation}
  \Psi = \sum_k c_k\psi_k.
  \label{eq:superposition}
\end{equation}
As long as the relevant branches remain phase-coherent, interference terms are
available.  A measurement interaction is the regime in which the probe, apparatus,
and surrounding medium cross a threshold where topology changes, phonons are
emitted, or persistent wake degrees of freedom are written---the same
irreversibility mechanism described for time in Paper~III~\cite{SSV-III}.
Reversing the event would require restoring the original phase relation across
all of those channels, not merely evolving a small isolated wave packet backward.

\subsection{Born weights as a working model}

The natural branch weights in \eqref{eq:superposition} are $|c_k|^2$ because
the local energy and intensity available to drive an apparatus response are
quadratic in the wave amplitude.  This gives the usual Born form
\begin{equation}
  P_k = |c_k|^2.
  \label{eq:born}
\end{equation}
At the present stage this is a consistency argument, not a full derivation.

\paragraph{Basin-volume derivation.}
\label{para:born-basin}
The basin argument is concrete enough to write down.  Let the apparatus
contain a reconnection-threshold degree of freedom that fires when the local
energy density driving it exceeds a fixed value~$\varepsilon_\star$, with
the rate of firing controlled by the perturbative coupling of the incoming
field to the threshold-bound mode.  For an incoming field
$\Psi = \sum_k c_k\,\psi_k$, the local energy density delivered to the
apparatus by branch~$k$ is $\varepsilon_k = |c_k|^2\,|\psi_k|^2$.  Near the
threshold, the firing rate is linear in the available energy density (the
leading term in the coupling; higher-order corrections are subleading in
$\varepsilon_k/\varepsilon_\star$ near the apparatus operating point):
\begin{equation}
  \Gamma_k = \kappa\,|c_k|^2,
  \qquad \kappa \equiv
    \int_{\text{apparatus}} \!\! g\,|\psi_k|^2\,\mathrm{d}V,
  \label{eq:gamma-k}
\end{equation}
where $g$ is the apparatus--field coupling kernel and the apparatus-volume
integral is the same for every branch $k$ that uses the same apparatus mode.
Over a short observation window $\tau$ with $\kappa\,\tau\ll 1$, the
probability that branch~$k$ has triggered the reconnection is
$\Gamma_k\,\tau = \kappa\,\tau\,|c_k|^2$, and the conditional probability
that branch~$k$ is the one that fires, given that exactly one branch fires
in the window, is
\begin{equation}
  P_k = \frac{\Gamma_k}{\sum_j \Gamma_j}
      = \frac{|c_k|^2}{\sum_j |c_j|^2}
      = |c_k|^2,
  \label{eq:born-derived}
\end{equation}
where the last step uses the standard normalisation $\sum_j |c_j|^2 = 1$.

\begin{gapbox}[Withdrawn: the basin argument does not derive the Born
exponent (\ssvissue{189})]
This paragraph previously concluded that ``the Born rule~\eqref{eq:born} is a
derived consequence of the SSV measurement mechanism, not merely a consistency
statement''.  That conclusion is withdrawn; the argument above is retained
because what it does establish is worth keeping.

The exponent is \emph{assumed, not obtained}.  The chain is
\[
  \varepsilon_k = |c_k|^2|\psi_k|^2
  \;\longrightarrow\;
  \Gamma_k \propto \varepsilon_k
  \;\longrightarrow\;
  P_k \propto |c_k|^2 ,
\]
and the first arrow is the Born weight itself --- the premise that the energy
density delivered by a branch is \emph{quadratic} in its amplitude.  It is
the same assumption the preceding paragraph offers as the informal
consistency argument.  Cancelling $\kappa$ removes a factor common to all
branches; it cannot change an exponent that entered at the first step.  This
is the error of \S\ref{sec:gausson-saturation} in a second place: what is put
in as an amplitude-squared is recovered as an amplitude-squared, and the
intervening algebra is where the appearance of derivation comes from.

What the argument \emph{does} establish, conditionally and usefully: given
(a) rate linear in delivered energy density, (b) a threshold mode common to
every branch, so that $\kappa$ is genuinely $k$-independent, and (c) unit
normalisation, the reconnection-threshold model reproduces Born weights
rather than some competing rule.  That is a real constraint on the
measurement model.  It is not a derivation of the exponent, which is what
makes the Born rule hard.

A derivation would have to obtain $\varepsilon_k \propto |c_k|^2$ from the
medium dynamics without assuming the quadratic form --- or replace the
argument with one that fixes the exponent from a symmetry or a counting
principle.  Neither is in hand.

Notation, recorded with it: $\kappa$ in \eqref{eq:gamma-k} is defined by an
integral containing $\psi_k$, so as written it carries a $k$ index that the
cancellation requires it not to have.  Assumption (b) is what removes it, and
it should be stated as an assumption rather than hidden in the notation.
\end{gapbox}

\subsection{What this does and does not claim}

This picture does not require consciousness, an external classical realm, or a
new postulate of collapse.  It also does not yet derive a complete theory of
detector statistics.  Its useful claim is more modest: SSV supplies a candidate
physical channel for irreversibility during measurement, namely topology change
and wake formation in the medium.  This is consistent with, but not identical
to, decoherence programmes that source irreversibility from environmental
entanglement; the SSV version sources it from physical medium events.

%%──────────────────────────────────────────────────────────────────────────────
\section{Discussion}
\label{sec:discussion}
%%──────────────────────────────────────────────────────────────────────────────

\subsection{Position in the series}

Paper~VII-a is the QM-limit paper in the SSV series.  Its inputs are the
Madelung structure of the LogSE (Papers~I--IV) and the topological defect picture
(Paper~I, Paper~V).  Its output---the Schr\"odinger limit, circulation
quantisation, hydraulic uncertainty, and reconnection measurement---is passed
forward as a standing result.  Claims about emergent geometry and the
Einstein-equation limit are in Paper~VII-b; cosmogony is in Paper~VIII.

\subsection{Relation to the Madelung and pilot-wave literature}

The Madelung~\cite{Madelung1927} representation is well-established; this is not
a new mathematical result.  The SSV contribution is the physical identification
of the medium: the Madelung fluid is not an abstract probability fluid but the
actual vacuum condensate, with the same medium that supports topological defects,
irreversible wakes, and gravitational gradients.  The difference from the de
Broglie--Bohm pilot-wave interpretation is that SSV does not introduce a guiding
wave on top of classical trajectories; the order parameter IS the physical medium
in which all dynamics occurs.

\subsection{Testable consequences}

Two near-term checks follow from the reconnection model.

\begin{enumerate}[label=\textbf{Q\arabic*.}]
  \item \textbf{Reconnection signatures in controlled measurement systems.}
    If detector clicks are accompanied by irreversible medium events, then
    sufficiently sensitive optomechanical or superconducting measurement
    platforms should show excess correlated acoustic, thermal, or phase-noise
    signatures at the transition from reversible coupling to recorded outcome.

  \item \textbf{Born-rule basin scaling.}
    A reduced reconnection-threshold model should test whether branch basin
    volumes scale as $|c_k|^2$ for simple two-channel apparatus couplings.
    This is the calculation needed before the measurement section can be
    promoted from physical interpretation to formal derivation.
\end{enumerate}

\subsection{Open problems}

\begin{enumerate}
  \item \emph{Bound-state spectra.}  Deriving the hydrogen-like spectrum directly
    from the LogSE, without treating $V_\mathrm{eff}$ as an external input, is
    part of the Paper~I programme.

  \item \emph{Precise uncertainty prefactor --- one route now closed.}  The
    $1/2$ in \eqref{eq:uncertainty} was to emerge from the LogSE
    coherent-state minimum.  That specific route is \emph{closed}, not
    deferred (\ssvissue{189}; \S\ref{sec:gausson-saturation}): on the branch
    Paper~I adopted the LogSE has no normalisable Gaussian stationary state,
    and on either branch the Gaussian uncertainty product is independent of
    the width and of $b$, so it cannot carry information about the LogSE.  A
    derivation of the prefactor would have to come from a \emph{topological}
    minimiser, and no candidate is in hand.

  \item \emph{Multi-particle states.}  The present treatment addresses
    single-order-parameter modes.  Multi-particle states and exchange statistics
    require a field-theoretic extension of the Madelung picture, not developed
    here.

  \item \emph{Spin-1/2 and Fermi statistics.}  The order parameter $\Psi$ is a
    scalar; half-integer spin and fermion anti-symmetry require an extension to
    spinor order parameters or a separate topological argument.  This is noted as
    a gap, not filled here.
\end{enumerate}

%%──────────────────────────────────────────────────────────────────────────────
\section{Conclusion}
\label{sec:conclusion}
%%──────────────────────────────────────────────────────────────────────────────

SSV~VII-a isolates the quantum-mechanics limit from the geometry and cosmogony
claims elsewhere in the series.  The paper's formal core is the Madelung bridge
and the Schr\"odinger limit.  Its interpretive core has three parts:

\begin{enumerate}
  \item Quantisation reflects phase topology: discrete winding numbers follow
    from the single-valuedness of the order parameter, not from a separate
    quantisation axiom.

  \item Uncertainty reflects hydraulic localisation stiffness: the quantum
    pressure~\eqref{eq:Q} penalises tight localisation, recovering the
    Heisenberg bound from the equation of state.

  \item Measurement reflects irreversible reconnection or wake-writing in the
    medium, sourcing irreversibility from topology change rather than from an
    external classical realm.
\end{enumerate}

That is sufficient to make the paper standalone without requiring it to carry
the Einstein-equation or cosmogony arguments.

%%──────────────────────────────────────────────────────────────────────────────
\bibliographystyle{unsrt}
\bibliography{../cited/references}

\appendix
% Auto-generated issue links and pinned repository references.
\input{provenance.tex}

\end{document}
