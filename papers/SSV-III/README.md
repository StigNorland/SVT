# SSV III

Working title: `Irreversible Time and Wake Entropy`

Scope: time as local change in `Psi`, the past as present wake structure, and entropy as persistent wake-complexity.

## Issue Status

GitHub issue `#1` is satisfied by the current draft:

- abstract rewritten around irreversible time and wake-complexity
- entropy framed as persistent wake-complexity, not a bare event count
- formal time reversal separated from physical reversal
- black-hole material moved out to `papers/SSV-V`
- CMB and primordial-phonon material moved out to `papers/SSV-IX`

Remaining work belongs to follow-on derivation issues, especially the computable wake functional `W[Psi]` and the thermodynamic coarse-graining bridge.

## Issue `#4` Status

GitHub issue `#4` is satisfied at the draft-formulation level:

- entropy is formulated as wake-complexity rather than event counting
- `main.tex` includes both schematic `W[Psi]` and statistical `Omega_wake[Psi]` forms
- monotonicity is stated as a physical inverse-process claim, not a bookkeeping convention
- local cancellation is separated from global unwriting
- reconnection is treated as one wake-writing channel, not the definition of entropy
- remaining dimensional and formal gaps are explicitly flagged in the entropy section

The remaining work is a later derivation problem: make the schematic wake channels computable and derive the coarse-graining map to thermodynamic entropy.
