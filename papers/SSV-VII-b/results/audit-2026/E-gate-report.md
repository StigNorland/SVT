# SSV-VII-b — E-gate report

Gate: **E-GATE PASS with two inherited items** · #190

## E1 — the $\sqrt2$ propagates into horizon counting

`main.tex:315` and `:362` use $\xi=\hbar/(m_0c)$ in horizon-area and entropy
counting. Under the adopted branch $\xi=\hbar/(\sqrt2 m_0c)$, so any quantity
scaling as $A_H/\xi^2$ moves by a **factor of 2**, and one going as $1/\xi$ by
$\sqrt2$.

This is a genuine numerical change, not a relabelling. It is the only place in
the series where the D1 $\sqrt2$ reaches a stated result rather than a
definition.

## E2 — inherited Bjerknes

`main.tex:202` presents the attractive Bjerknes acoustic force without the #119
falsification. Third occurrence in the series.

## Credit — the sign was right here

`main.tex:144` uses $\Phi=+b\ln(\rho/\rho_0)$, required for
$\Phi\approx-GM/r$ and **opposite to SSV-I as printed**. It agrees with the
corrected D1 branch. SSV-VII-b needs no sign change; SSV-I did.
